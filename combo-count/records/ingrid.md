# イングリッド コンボカウント実測記録

## ODサンフレアLv3

- タイムスタンプ: 2026-07-28 14:03 +0900
- コンボ列: 立ち状態のヒットから ODサンフレアLv3 を当て、浮いた相手に当て続ける
- 結果: **101発ヒットして途切れ、102発目がすかった**
- 導出:
  - 始動は加算しないので、1発目のカウントは始動値 0 のまま。k 発目(k≥2)が判定で見るカウントは `k-2`。
  - 101 発目は C=99 で通り、102 発目は C=100 で弾かれた → **上限値 = 99**(始動値 0、加算値 1 の正規化の下)。
  - 始動後に入るビームがちょうど 100 発。カウントが 0 始まりなので内部の上限値が 99 になる。
- 未確定・注意:
  - 単一技ループなので**加算値と上限値は分離不能**。得られるのは両者の比のみ。「加算値 = 1」は設計意図からの推論(README の「単位の確定」を参照)。
  - 「始動値 = 0」は本技をこの始動で当てたときのローカル原点。共通の DI 始動原点との対応は未タイ。
