# イングリッド コンボカウント実測記録

## ODサンフレアLv3

- タイムスタンプ: 2026-07-28 14:03 +0900
- コンボ列: 立ち状態のヒットから ODサンフレアLv3 を当て、浮いた相手に当て続ける
- 結果: **101発ヒットして途切れ、102発目がすかった**
- 導出:
  - この実験で直接得られるのは `(上限値 − 始動値) / 加算値 ∈ [100, 101)`。整数読みで **100**。
  - 正規化(ゲージ)を「始動値 = 0、加算値 = 1」と置くと **上限値 = 100**。
  - 数え方の規約: **1発目(立ち始動)を始動値の位置に置き、当たるたびに加算値を足す**方式を採用(この方式で101発 → 上限100)。全記録でこの規約に統一する。
- 未確定・注意:
  - 単一技ループなので**加算値と上限値は分離不能**。得られるのは両者の比のみ。「加算値 = 1」は本技を単位に選んだ仮定で、共通の gcd=1 単位と一致するかは未検証。
  - 「始動値 = 0」は本技をこの始動で当てたときのローカル原点。共通の DI 始動原点との対応は未タイ。
  - 上限値がちょうど 100 か、100.x を切り捨てて 100 に見えているだけかは本実験単体では決められない(設計上は整数と見るのが自然)。
